version: "3.7"
services:
  front:
    build:
      context: ./front
      dockerfile: Dockerfile
    container_name: foresight_front
    ports:
      - "2443:2443"
    env_file:
      - ./front/env.properties
    restart: "always"